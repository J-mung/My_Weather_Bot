# My Weather Bot 로컬 에이전트 규칙

이 파일은 로컬 전용 에이전트 진입점이다. 공개 저장소에 올리지 않는다.

## 필수 읽기 순서

1. 작업 시작 전 `./AGENT/.AGENTS.md`를 먼저 읽는다.
2. 현재 진행 방향과 다음 우선순위는 `/Users/J_mung/Works/ToyProjects/기상앱_자료/10_로드맵/고도화_로드맵.md`를 기준으로 확인한다.
3. 에이전트 전용 문서나 로드맵을 갱신해야 하면 `./AGENT/` 또는 `/Users/J_mung/Works/ToyProjects/기상앱_자료` 아래 파일만 수정한다.

## 공개 노출 금지

- `./AGENT/`, `./AGENTS.md`, `./.AGENTS.md`, `/Users/J_mung/Works/ToyProjects/기상앱_자료/00_운영`, `/Users/J_mung/Works/ToyProjects/기상앱_자료/10_로드맵`은 로컬 전용 문서 공간이다.
- 에이전트용 세부 로드맵, 작업 규칙, 내부 문서 경로를 `README.md` 같은 공개 문서로 복사하지 않는다.
- 사용자가 명시적으로 요청하지 않는 한 `./AGENT/`, `./AGENTS.md`, `./.AGENTS.md`를 `git add -f` 하지 않는다.

## 사용자 노출 문구 원칙

- API 키 값은 물론 키 이름, 환경변수명, 설정 경로처럼 사용자가 알 필요 없는 민감 설정 정보도 콘솔·알림·오류 화면·문구에 노출하지 않는다.
- 사용자 화면에는 API 호출 절감, 캐시, 레이트 리밋, 자동 조회 제거 같은 내부 구현 사유를 설명하는 문구를 노출하지 않는다.
- 성능/한도 대응 내용은 코드와 작업 문서에 남기고, UI 문구는 예보 확인·현재 위치·북마크 관리처럼 사용자가 직접 이해할 수 있는 가치만 표현한다.

## 오류 코드와 안내 문구 중앙 관리

- 사용자에게 표시되는 오류 코드와 안내 문구는 `src/shared/api/app-errors.ts`에서 중앙 관리한다.
  - 새 오류 유형은 `APP_ERROR`, `APP_ERROR_CODE`, `appErrorMetaMap`에 함께 추가한다.
  - 화면/훅/서비스 파일에 사용자 노출 오류 코드나 안내 문구를 임의 상수로 흩뿌리지 않는다.
  - 환경변수 키 이름, API 키, URL, 내부 프록시 경로, 설정 파일 경로 같은 민감 설정 정보는 오류 문구와 코드에 포함하지 않는다.
- 에러 코드 표시 UI는 `src/shared/ui/error-code/ErrorCode.tsx`를 사용한다.
  - `에러 코드:` 라벨, 스타일, 표시 여부는 이 컴포넌트에서 관리한다.
  - 각 화면에서는 `code`만 전달하고 동일한 마크업을 직접 작성하지 않는다.
- 오류 안내 화면은 사용자에게는 복구 가능한 행동만 안내하고, 개발자가 대처할 식별자는 `MWB-도메인-번호` 형식의 안전한 코드로 제공한다.
- 오류 화면 강제 확인용 임시 코드를 추가했다면 검증 후 커밋 전에 반드시 제거한다.
